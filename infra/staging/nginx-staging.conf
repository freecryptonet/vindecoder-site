# /etc/nginx/sites-available/staging.vindecoder.site
# Symlink to /etc/nginx/sites-enabled/ then `nginx -t && systemctl reload nginx`.

server {
    listen 80;
    listen [::]:80;
    server_name staging.vindecoder.site;

    # Allow Let's Encrypt HTTP-01 challenge before redirecting.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name staging.vindecoder.site;

    ssl_certificate     /etc/letsencrypt/live/staging.vindecoder.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.vindecoder.site/privkey.pem;

    # Block staging from indexing.
    add_header X-Robots-Tag "noindex, nofollow" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # Block ClaudeBot and other AI scrapers at the edge (per
    # reference_anthropic_claudebot memo from the archive).
    if ($http_user_agent ~* "ClaudeBot|anthropic-ai|GPTBot|CCBot|Google-Extended") {
        return 403;
    }

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
        proxy_connect_timeout 30s;
    }
}
